# Nepher Docker Environment Variables
# Copy to .env and customize

# Optional: Custom API URL (default: https://tournament-api.nepher.ai)
# NEPHER_API_URL=https://tournament-api.nepher.ai

# Optional: Custom wallet path (default: ~/.bittensor)
# BITTENSOR_WALLET_PATH=~/.bittensor

# Optional: Wallet configuration
# WALLET_NAME=validator
# WALLET_HOTKEY=default

# Optional: Workspace paths
# NEPHER_WORKSPACE=./workspace
# NEPHER_ENV_CACHE=~/.cache/nepher

# Optional: Evaluation repo Git URL (default: https://github.com/nepher-ai/eval-nav.git)
# EVAL_REPO_URL=https://github.com/nepher-ai/eval-nav.git
