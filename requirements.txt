# Nepher Subnet Dependencies
# Core dependencies
# Floor raised to 10.5.0: finney runtime spec_version 432 wraps the
# neurons_lite netuid param in a Composite; older bittensor / ASI <2
# encode it as a bare u16 and metagraph sync fails with
# "Invalid type for data ... Composite".
bittensor>=10.5.0,<11
bittensor-wallet>=4.1.0
async-substrate-interface>=2.2.1,<3
httpx>=0.25.0
pyyaml>=6.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
tenacity>=8.2.0
structlog>=23.0.0
aiofiles>=23.0.0
python-dotenv>=1.0.0

# Validator-specific (uncomment for validator)
# nepher>=0.1.0

